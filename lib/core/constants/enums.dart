enum SnackBarType { error, success, warning }

enum FoodTag {
  normal,
  plate,
  inMenu,
  special,
  favorite,
  persian,
  nonPersian,
  pizza,
  sandwich,
}

enum CartStatus {
  cart,
  completeInfo,
  checkout,
}

enum PaymentMethods {
  saman,
  melat,
  parsian,
}

enum InternetConnectionType {
  mobile,
  wifi,
  ethernet,
  vpn,
}

enum OrderStatus {
  canceled,
  delivered,
  pending
}
